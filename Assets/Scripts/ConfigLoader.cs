using UnityEngine;

// Superseded by SimConfig ScriptableObject + CrowdManager. Kept to avoid
// missing-script warnings on ___SimController. Does nothing at runtime.
public class ConfigLoader : MonoBehaviour { }